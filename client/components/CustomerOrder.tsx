import '../../pico.sand.css'

function CustomerOrder() {

  // TODO: figure out how to pass randomly generated info as props into the customer order component
  // TODO: fgure out what the file structure should look like
  //  - There is the button click on the header component, that will have a handleButtonClick.
  //  - This handle button click will randomly generate an order size,
  //  - then use that number to randomly select items off of the menu.
  //  - Those items will then get passed into this component, and this component will process and display them as html
  //  - this component will then get called on the customerOrderModal.tsx
  return (
    <>
    </>
  )
}

export default CustomerOrder
